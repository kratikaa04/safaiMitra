# SafaiMitra Smart Dustbin — Python dependencies
# Install with:  pip install -r requirements.txt
#
# Tested on Windows 11 / Python 3.10+
# TensorFlow 2.x ships with Keras built-in (no separate keras install needed)

tensorflow==2.15.0
numpy
opencv-python
pyserial
requests
